# sing-box_config
自用的sing-box配置

需要使用reF1nd版内核使用

singbox-manager.ps1为sing-box.exe管理脚本（后台运行版本），需放在与sing-box.exe同一目录下

start-manager.bat为启动脚本程序（cmd命令提示符），可放在任意位置，可编辑cd /d ""

start-manager_2.bat为启动Windows“终端”cmd版本，美化一点，内存占用高点

sing-box_S1mon.json为现用配置
